import { SoletrandoPipe } from './soletrando.pipe';

describe('SoletrandoPipe', () => {
  it('create an instance', () => {
    const pipe = new SoletrandoPipe();
    expect(pipe).toBeTruthy();
  });
});
